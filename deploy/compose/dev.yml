services:
  app:
    container_name: bee-app
    build:
      context: ../../
      dockerfile: deploy/dockerfile/fastapi/Dockerfile
    volumes:
      - ../../codebase/app:/app/app
    env_file:
      - ../variables/reference
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - bee
    links:
      - postgres
    restart: unless-stopped
    command: >
      uvicorn app.app:app
      --host 0.0.0.0
      --port 5000
      --reload
      --reload-dir /app/app
    healthcheck:
      test: ["CMD-SHELL", "curl -f http://localhost:5000/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 3
      start_period: 15s

  postgres:
    container_name: bee-postgres
    restart: unless-stopped
    build:
      context: ../dockerfile/postgres
      dockerfile: Dockerfile
    volumes:
      - ../dockerfile/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
      - postgres:/var/lib/postgresql/data
    env_file:
      - ../variables/reference
    command: ["postgres", "-c", "listen_addresses=*"]
    healthcheck:
      test:
        [
          "CMD",
          "pg_isready",
          "-U",
          "bee_user",
          "-d",
          "bee_db",
          "-h",
          "localhost",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 15s
    networks:
      - bee

  nginx:
    container_name: bee-nginx
    restart: unless-stopped
    build:
      context: ../dockerfile/nginx
      dockerfile: Dockerfile
    ports:
      - "8000:443"
    depends_on:
      app:
        condition: service_healthy
    networks:
      - bee

volumes:
  postgres:

networks:
  bee:
    driver: bridge
